---
phase: "01-foundation"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - "src/components/Header.astro"
  - "src/components/Footer.astro"
  - "src/layouts/Layout.astro"
autonomous: false
requirements:
  - "PHASE-1-DELIVERABLES"
  - "D-04"
  - "D-05"
  - "D-06"
user_setup: []
threat_model:
  security_enforcement: true

must_haves:
  truths:
    - "Header is visible on all pages with logo, nav links, and click-to-call button"
    - "Click-to-call button uses tel: href and is visible on both mobile and desktop"
    - "Header is sticky (position: sticky, top: 0) and shows box-shadow on scroll"
    - "Mobile nav hamburger toggle is visible at base resolution and hidden at sm (768px+)"
    - "Nav links are hidden at base and visible as flex row at sm+"
    - "Footer displays 'A Page and Pixel brand' attribution in accent color"
    - "Footer has nav links matching header navigation"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Sticky header with logo, nav, click-to-call button, mobile hamburger"
      contains: "position: sticky, tel: href, aria-expanded, aria-label"
    - path: "src/components/Footer.astro"
      provides: "Footer with brand, nav, contact info, Page and Pixel attribution"
      contains: "Page and Pixel brand"
    - path: "src/layouts/Layout.astro"
      provides: "Layout now imports and renders Header and Footer"
      contains: "Header, Footer"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/components/Header.astro"
      via: "import and usage in body"
    - from: "src/layouts/Layout.astro"
      to: "src/components/Footer.astro"
      via: "import and usage in body"
---

<objective>
Create Header.astro (logo + nav + sticky + click-to-call) and Footer.astro (brand + nav + contact + Page and Pixel attribution). Wire both into Layout.astro from Plan 01.

Purpose: Shared navigation and footer components that appear on every page.
Output: src/components/Header.astro, src/components/Footer.astro, updated Layout.astro
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UI-SPEC.md (Component Inventory — Site Header and Site Footer sections)
@.planning/phases/01-foundation/01-RESEARCH.md (Patterns 4 and 5 — sticky header and footer)
@.planning/phases/01-foundation/01-01-SUMMARY.md (if exists, to understand Layout wiring)
@src/layouts/Layout.astro (from Plan 01 — now add Header/Footer imports and usage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Layout.astro to import and render Header and Footer</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>
    - src/layouts/Layout.astro (existing file from Plan 01)
  </read_first>
  <action>
    Edit `src/layouts/Layout.astro` to add Header and Footer imports and usage.

    In the frontmatter section (the --- block), add these imports at the top:
    ```astro
    import Header from '../components/Header.astro';
    import Footer from '../components/Footer.astro';
    ```

    In the `<body>`, add `<Header />` after the skip link and `<Footer />` after the closing `</main>` tag:
    ```html
    <body>
      <a href="#main-content" class="skip-link">Skip to main content</a>
      <Header />
      <main id="main-content">
        <slot />
      </main>
      <Footer />
    </body>
    ```

    The imports and component usage must be added — do NOT recreate the entire file.
  </action>
  <verify>
    <automated>grep -c "import Header" src/layouts/Layout.astro && grep -c "import Footer" src/layouts/Layout.astro && grep -c "<Header" src/layouts/Layout.astro && grep -c "<Footer" src/layouts/Layout.astro</automated>
  </verify>
  <done>
    Layout.astro imports Header.astro and Footer.astro, and renders both components in the body. Header appears after skip link, Footer after main content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Header.astro component</name>
  <files>src/components/Header.astro</files>
  <read_first>
    - src/styles/global.css (verify CSS custom properties are available)
  </read_first>
  <action>
    Create `src/components/Header.astro` with the following implementation:

    <strong>Component Structure:</strong>
    - `.site-header` wrapper: `position: sticky; top: 0; z-index: 100; background: var(--color-surface);`
    - `.header-inner` container with max-width 1200px, centered
    - Logo: text-based "Oak Ridge" (primary color, Roboto Slab 700) with "Roofing Co." on second line
    - Nav links at sm+: Home, Services, Roof Repair, Storm Damage, Contact
    - Click-to-call button: accent background, primary text, phone icon (SVG), "Call Now" label
    - Mobile hamburger: 44x44px touch target, shows at base, hidden at sm+

    <strong>CSS Implementation (in the component's `<style>` tag):</strong>

    ```css
    .site-header {
      position: sticky;
      top: 0;
      z-index: 100;
      background: var(--color-surface);
      box-shadow: var(--shadow-sm);
      transition: box-shadow 200ms ease;
    }

    .header-inner {
      display: flex;
      align-items: center;
      justify-content: space-between;
      max-width: 1200px;
      margin: 0 auto;
      padding: var(--space-4);
      gap: var(--space-4);
    }

    .logo {
      display: flex;
      flex-direction: column;
      text-decoration: none;
      color: var(--color-primary);
    }

    .logo-primary {
      font-family: var(--font-headline);
      font-size: 1.25rem;
      font-weight: 700;
      line-height: 1.1;
    }

    .logo-secondary {
      font-family: var(--font-headline);
      font-size: 0.875rem;
      font-weight: 400;
      color: var(--color-text-muted);
    }

    /* Desktop nav — hidden on mobile */
    .nav-desktop {
      display: none;
      gap: var(--space-8);
    }

    @media (min-width: 48rem) {
      .nav-desktop {
        display: flex;
      }
    }

    .nav-desktop a {
      font-family: var(--font-body);
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--color-text);
      text-decoration: none;
      transition: color 150ms ease;
    }

    .nav-desktop a:hover,
    .nav-desktop a:focus-visible {
      color: var(--color-accent);
    }

    /* CTA Button */
    .btn-call {
      display: none;
      align-items: center;
      gap: var(--space-2);
      background: var(--color-accent);
      color: var(--color-primary);
      font-family: var(--font-body);
      font-size: 0.875rem;
      font-weight: 600;
      padding: var(--space-2) var(--space-4);
      border-radius: var(--radius-md);
      text-decoration: none;
      min-height: 44px;
      transition: filter 150ms ease;
    }

    .btn-call:hover,
    .btn-call:focus-visible {
      filter: brightness(0.95);
    }

    @media (min-width: 48rem) {
      .btn-call {
        display: flex;
      }
    }

    /* Mobile toggle — visible on mobile */
    .nav-mobile-toggle {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      background: transparent;
      border: none;
      cursor: pointer;
      color: var(--color-text);
      border-radius: var(--radius-sm);
    }

    .nav-mobile-toggle:hover {
      background: rgba(93, 78, 55, 0.06);
    }

    @media (min-width: 48rem) {
      .nav-mobile-toggle {
        display: none;
      }
    }
    ```

    <strong>HTML Structure:</strong>
    ```astro
    <header class="site-header">
      <div class="header-inner">
        <a href="/" class="logo" aria-label="Oak Ridge Roofing Co. — Home">
          <span class="logo-primary">Oak Ridge</span>
          <span class="logo-secondary">Roofing Co.</span>
        </a>

        <nav class="nav-desktop" aria-label="Main navigation">
          <a href="/">Home</a>
          <a href="/services">Services</a>
          <a href="/repair">Roof Repair</a>
          <a href="/storm-damage">Storm Damage</a>
          <a href="/contact">Contact</a>
        </nav>

        <a href="tel:+15551234567" class="btn-call">
          <svg aria-hidden="true" width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
            <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
          </svg>
          <span>Call Now</span>
        </a>

        <button class="nav-mobile-toggle" aria-label="Open menu" aria-expanded="false" type="button">
          <svg aria-hidden="true" width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M3 18h18v-2H3v2zm0-5h18v-2H3v2zm0-7v2h18V6H3z"/>
          </svg>
        </button>
      </div>
    </header>
    ```

    Note: Mobile menu toggle requires JavaScript for interactivity (expanded/collapsed state). This is a minimal implementation — the mobile menu behavior (slide-down navigation) will be implemented when content is added in later phases. For Phase 1, the toggle button exists as a structural element with correct ARIA attributes.
  </action>
  <verify>
    <automated>grep -c "site-header" src/components/Header.astro && grep -c "position: sticky" src/components/Header.astro && grep -c "tel:" src/components/Header.astro</automated>
  </verify>
  <done>
    Header.astro exists with logo (Oak Ridge + Roofing Co.), nav links (Home, Services, Repair, Storm Damage, Contact), click-to-call button with tel: link and SVG phone icon, sticky positioning with shadow, mobile hamburger toggle with correct ARIA attributes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Footer.astro component</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/styles/global.css (verify CSS custom properties are available)
  </read_first>
  <action>
    Create `src/components/Footer.astro` with the following implementation:

    <strong>Component Structure:</strong>
    - Background: var(--color-primary) — dark brown
    - Text color: var(--color-surface) — white
    - Three-column layout at desktop (brand, nav, contact), single column at mobile
    - "A Page and Pixel brand" attribution in accent color, with hover effect

    <strong>CSS Implementation (in the component's `<style>` tag):</strong>

    ```css
    .site-footer {
      background: var(--color-primary);
      color: var(--color-surface);
      padding: var(--space-12) 0;
    }

    @media (min-width: 48rem) {
      .site-footer {
        padding: var(--space-16) 0;
      }
    }

    .footer-inner {
      display: flex;
      flex-direction: column;
      gap: var(--space-8);
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 var(--space-4);
    }

    @media (min-width: 48rem) {
      .footer-inner {
        flex-direction: row;
        justify-content: space-between;
        padding: 0 var(--space-8);
      }
    }

    .footer-brand {
      display: flex;
      flex-direction: column;
      gap: var(--space-2);
    }

    .brand-name {
      font-family: var(--font-headline);
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--color-surface);
    }

    .brand-attribution {
      font-size: 0.875rem;
      color: var(--color-accent);
      transition: opacity 150ms ease;
    }

    .brand-attribution:hover {
      opacity: 0.8;
    }

    .footer-nav {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .footer-nav a {
      font-family: var(--font-body);
      font-size: 0.875rem;
      color: var(--color-surface);
      text-decoration: none;
      transition: color 150ms ease;
    }

    .footer-nav a:hover,
    .footer-nav a:focus-visible {
      color: var(--color-accent);
    }

    .footer-contact {
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
    }

    .footer-contact p {
      font-size: 0.875rem;
      margin: 0;
    }

    .footer-contact a {
      color: var(--color-accent);
      text-decoration: none;
      transition: opacity 150ms ease;
    }

    .footer-contact a:hover {
      opacity: 0.8;
    }
    ```

    <strong>HTML Structure:</strong>
    ```astro
    <footer class="site-footer">
      <div class="footer-inner">
        <div class="footer-brand">
          <span class="brand-name">Oak Ridge Roofing Co.</span>
          <span class="brand-attribution">A Page and Pixel brand</span>
        </div>

        <nav class="footer-nav" aria-label="Footer navigation">
          <a href="/">Home</a>
          <a href="/services">Services</a>
          <a href="/repair">Roof Repair</a>
          <a href="/storm-damage">Storm Damage</a>
          <a href="/contact">Contact</a>
        </nav>

        <div class="footer-contact">
          <p><a href="tel:+15551234567">(555) 123-4567</a></p>
          <p>info@oakridgeroofing.com</p>
          <p>Serving the greater Oak Ridge area</p>
        </div>
      </div>
    </footer>
    ```
  </action>
  <verify>
    <automated>grep -c "site-footer" src/components/Footer.astro && grep -c "Page and Pixel" src/components/Footer.astro && grep -c "color-surface" src/components/Footer.astro</automated>
  </verify>
  <done>
    Footer.astro exists with brand name (Oak Ridge Roofing Co.), "A Page and Pixel brand" attribution, nav links matching header, contact info (phone, email, service area), dark primary background with surface text.
  </done>
</task>

</tasks>

<verification>
[overall phase checks]
- npm run build completes without errors (Header/Footer now wired)
- Header visible on all pages via Layout.astro import
- Footer visible on all pages via Layout.astro import
- Click-to-call uses tel: protocol for mobile
- Page and Pixel attribution in footer
</verification>

<success_criteria>
Header.astro and Footer.astro are created with correct design token usage, responsive behavior (mobile-first breakpoints at 768px), and accessibility attributes. Both components are imported and rendered in Layout.astro. `npm run build` completes without errors.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>