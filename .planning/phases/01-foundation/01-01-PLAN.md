---
phase: "01-foundation"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "package.json"
  - "astro.config.mjs"
  - "tsconfig.json"
  - "src/styles/global.css"
  - "src/layouts/Layout.astro"
  - "src/pages/index.astro"
autonomous: false
requirements:
  - "PHASE-1-DELIVERABLES"
  - "D-01"
  - "D-02"
  - "D-06"
  - "D-08"
  - "D-09"
  - "D-10"
user_setup: []
threat_model:
  security_enforcement: true

must_haves:
  truths:
    - "npm run build completes without errors"
    - "All pages share consistent Layout.astro wrapper"
    - "Design tokens (colors, typography, spacing) available as CSS custom properties on :root"
    - "Roboto Slab font is available via --font-headline CSS variable"
    - "Archivo font is available via --font-body CSS variable"
    - "Global CSS loaded once in Layout.astro, not per-page"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config with Google Fonts fontProviders"
      contains: "fontProviders.google()"
    - path: "src/styles/global.css"
      provides: "Design tokens, responsive breakpoints, base typography"
      contains: "--color-primary, --color-secondary, --color-background, --color-surface, --color-text, --color-text-muted, --color-accent"
    - path: "src/layouts/Layout.astro"
      provides: "Base layout wrapping all pages (HTML shell with slot)"
      contains: "Font, global.css, skip-link, main#main-content, slot"
    - path: "src/pages/index.astro"
      provides: "Home page stub using Layout.astro"
  key_links:
    - from: "src/layouts/Layout.astro"
      to: "src/styles/global.css"
      via: "import '../styles/global.css'"
    - from: "src/layouts/Layout.astro"
      to: "astro.config.mjs"
      via: "Font components loaded via astro:assets"
---

<objective>
Initialize the Astro project with proper structure, create the global CSS design system with all tokens, create the base Layout.astro (HTML shell without Header/Footer), and configure Google Fonts via astro.config.mjs.

Purpose: Establish the foundation that all subsequent phases build upon.
Output: Scaffolding complete, design tokens in global.css, base Layout.astro with font configuration (Header/Footer wiring deferred to Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro project scaffold</name>
  <files>package.json, astro.config.mjs, tsconfig.json, src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (will be created)
  </read_first>
  <action>
    Run `npm create astro@latest . -- --template minimal --yes --no-install` to initialize Astro in the current directory using the minimal template.

    After initialization, update `package.json` scripts to include:
    - `"dev": "astro dev"` (should already exist from template)
    - `"build": "astro build"`
    - `"preview": "astro preview"`

    Update `astro.config.mjs` to use the proper configuration with Google Fonts provider (see pattern below).

    Create `tsconfig.json` if not present from template (Astro's minimal template includes it).

    <strong>astro.config.mjs content:</strong>
    ```
    import { defineConfig, fontProviders } from "astro/config";

    export default defineConfig({
      fonts: [
        {
          provider: fontProviders.google(),
          name: "Archivo",
          cssVariable: "--font-body",
        },
        {
          provider: fontProviders.google(),
          name: "Roboto Slab",
          cssVariable: "--font-headline",
        },
      ],
    });
    ```

    <strong>package.json scripts (verify these exist):</strong>
    ```json
    {
      "scripts": {
        "dev": "astro dev",
        "build": "astro build",
        "preview": "astro preview",
        "astro": "astro"
      }
    }
    ```

    Do NOT install additional npm packages — Astro template already includes everything needed.
  </action>
  <verify>
    <automated>ls package.json astro.config.mjs tsconfig.json src/pages/index.astro 2>/dev/null && echo "SCAFFOLD-OK"</automated>
  </verify>
  <done>
    Astro project scaffold exists with minimal template, package.json has dev/build/preview scripts, astro.config.mjs has Google Fonts configuration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create global.css with design tokens and responsive breakpoints</name>
  <files>src/styles/global.css</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (design tokens section)
  </read_first>
  <action>
    Create `src/styles/global.css` with ALL of the following sections:

    <strong>1. CSS Reset and Box Sizing:</strong>
    ```css
    *, *::before, *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    ```

    <strong>2. Design Tokens on :root:</strong>
    ```css
    :root {
      /* Colors per D-01 and UI-SPEC */
      --color-primary: #5D4E37;
      --color-secondary: #3D5A45;
      --color-background: #F5F0E8;
      --color-surface: #FFFFFF;
      --color-text: #2C2416;
      --color-text-muted: #6B5D4D;
      --color-accent: #C4A35A;

      /* Typography per D-02 — font variables map to astro.config.mjs cssVariable */
      --font-headline: 'Roboto Slab', serif;
      --font-body: 'Archivo', sans-serif;

      /* Typography scale — mobile → desktop */
      --font-size-headline-lg: 2.25rem;  /* 36px desktop */
      --font-size-headline-md: 1.75rem;  /* 28px mobile */
      --font-size-headline-xl: 3rem;     /* 48px desktop hero */

      --font-size-subheading-lg: 1.875rem; /* 30px desktop */
      --font-size-subheading-md: 1.25rem;  /* 20px mobile */

      --font-size-body-lg: 1.125rem;   /* 18px desktop */
      --font-size-body-md: 1rem;       /* 16px mobile */
      --font-size-body-xl: 1.25rem;   /* 20px desktop */

      --font-size-caption: 0.75rem;    /* 12px */

      /* Spacing scale — 8-point system */
      --space-1: 0.25rem;   /* 4px */
      --space-2: 0.5rem;    /* 8px */
      --space-3: 0.75rem;   /* 12px */
      --space-4: 1rem;      /* 16px */
      --space-6: 1.5rem;    /* 24px */
      --space-8: 2rem;      /* 32px */
      --space-12: 3rem;     /* 48px */
      --space-16: 4rem;     /* 64px */
      --space-24: 6rem;     /* 96px */

      /* Border radius */
      --radius-sm: 0.25rem;  /* 4px */
      --radius-md: 0.5rem;   /* 8px */
      --radius-lg: 1rem;    /* 16px */

      /* Shadows */
      --shadow-sm: 0 1px 4px rgba(44, 36, 22, 0.06);
      --shadow-md: 0 4px 12px rgba(44, 36, 22, 0.10);
      --shadow-lg: 0 8px 24px rgba(44, 36, 22, 0.12);

      /* Breakpoints per D-06 — CSS custom properties for reference, but media queries use hard-coded values */
      --bp-sm: 48rem;   /* 768px */
      --bp-md: 64rem;   /* 1024px */
      --bp-lg: 90rem;   /* 1440px */
    }
    ```

    <strong>3. Base Typography Styles:</strong>
    ```css
    body {
      font-family: var(--font-body);
      font-size: var(--font-size-body-md);
      color: var(--color-text);
      background-color: var(--color-background);
      line-height: 1.5;
    }

    h1, h2, h3, h4, h5, h6 {
      font-family: var(--font-headline);
      line-height: 1.2;
    }

    h1 { font-size: var(--font-size-headline-md); font-weight: 700; }
    h2 { font-size: var(--font-size-headline-md); font-weight: 700; }
    h3 { font-size: var(--font-size-subheading-md); font-weight: 600; }

    p { margin-bottom: var(--space-4); }

    a {
      color: var(--color-primary);
      text-decoration: none;
    }
    ```

    <strong>4. Container System:</strong>
    ```css
    .container {
      width: 100%;
      max-width: 1200px;
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--space-4);
      padding-right: var(--space-4);
    }

    @media (min-width: 48rem) {
      .container {
        padding-left: var(--space-8);
        padding-right: var(--space-8);
      }
    }

    @media (min-width: 64rem) {
      .container {
        padding-left: var(--space-12);
        padding-right: var(--space-12);
      }
    }
    ```

    <strong>5. Responsive Typography Scale:</strong>
    ```css
    @media (min-width: 48rem) {
      body { font-size: var(--font-size-body-lg); }
      h1 { font-size: var(--font-size-headline-lg); }
      h2 { font-size: var(--font-size-headline-lg); }
      h3 { font-size: var(--font-size-subheading-lg); }
    }

    @media (min-width: 64rem) {
      body { font-size: var(--font-size-body-xl); }
    }
    ```

    <strong>6. Accessibility — Skip Link and Focus Styles:</strong>
    ```css
    .skip-link {
      position: absolute;
      top: -40px;
      left: 0;
      background: var(--color-accent);
      color: var(--color-primary);
      padding: var(--space-2) var(--space-4);
      z-index: 9999;
      transition: top 200ms ease;
    }

    .skip-link:focus {
      top: 0;
    }

    :focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
    }
    ```

    <strong>7. Utility Classes:</strong>
    ```css
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "color-primary" src/styles/global.css && grep -c "font-headline" src/styles/global.css && grep -c "@media (min-width" src/styles/global.css</automated>
  </verify>
  <done>
    global.css exists with all design tokens (colors, typography scale, spacing), responsive breakpoints (768px, 1024px, 1440px), and base typography applied via CSS custom properties on :root.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create base Layout.astro (HTML shell, no Header/Footer yet)</name>
  <files>src/layouts/Layout.astro</files>
  <action>
    Create `src/layouts/Layout.astro` with the HTML document shell — NO Header/Footer imports or component usage yet. Header and Footer will be added in Plan 02 when those components are created.

    ```astro
    ---
    import { Font } from 'astro:assets';
    import '../styles/global.css';

    interface Props {
      title: string;
      description?: string;
    }

    const { title, description = 'Premium roofing services by Oak Ridge Roofing Co.' } = Astro.props;
    ---

    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <meta name="description" content={description} />
      <title>{title} | Oak Ridge Roofing Co.</title>
      <Font cssVariable="--font-body" />
      <Font cssVariable="--font-headline" />
      <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    </head>
    <body>
      <a href="#main-content" class="skip-link">Skip to main content</a>
      <main id="main-content">
        <slot />
      </main>
    </body>
    </html>
    ```

    The Header and Footer components will be imported and rendered in Plan 02 when those files are created. This plan creates only the HTML shell with the slot.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|Error|warning|built)" | head -5</automated>
  </verify>
  <done>
    Layout.astro created with Font components loading --font-body and --font-headline from astro:assets, global CSS imported, skip-to-content link, and main#main-content slot. Header/Footer imports are NOT present — they are added in Plan 02.
  </done>
</task>

</tasks>

<verification>
[overall phase checks]
- npm run build completes without errors
- All pages share consistent Layout.astro wrapper
- Design tokens applied via var() throughout components
</verification>

<success_criteria>
Phase 1 scaffold is functional: `npm run build` completes without errors, Layout.astro wraps all pages, global.css provides all design tokens as CSS custom properties, astro.config.mjs configures Google Fonts for Roboto Slab and Archivo. Header and Footer will be wired to Layout.astro in Plan 02.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>